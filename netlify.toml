[build]
  publish = "."
  command = ""

# Redirect old landing URL to site root
[[redirects]]
  from = "/landing.html"
  to = "/"
  status = 301
  force = true

# Recommended: update publish directory if you add a build step later
# For static site now, base = root and publish = "." is correct.
